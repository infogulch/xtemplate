#!/usr/bin/env bash
#MISE description="Print content hash of go version + go.mod+go.sum for cache keys"
set -euo pipefail
# shellcheck source=/dev/null
source "$MISE_PROJECT_ROOT/.config/mise/lib.sh"
cd "$ROOT"

xt_prebuild_hash
