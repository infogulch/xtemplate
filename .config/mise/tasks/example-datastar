#!/usr/bin/env nu
#MISE description="Run the datastar example (http://localhost:9007)"
#MISE depends=["build-cli"]
use ../lib.nu *
cd ($env.ROOT | path join examples datastar)
^($env.DIST_DIR | path join xtemplate) --config-file config.json --listen :9007
