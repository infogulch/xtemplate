#!/usr/bin/env bash
#MISE description="Shellcheck the mise task scripts"
set -euo pipefail
# shellcheck source=/dev/null
source "$MISE_PROJECT_ROOT/.config/mise/lib.sh"
cd "$ROOT"

shellcheck .config/mise/lib.sh .config/mise/tasks/*
