#!/usr/bin/env nu
#MISE description="Build Caddy with the xtemplate module via xcaddy"
use ../lib.nu *

# Any extra args are forwarded to `xcaddy build`.
def main [...extra: string] {
    xt-build-caddy ...$extra
}
