#!/usr/bin/env nu
#MISE description="Run the filebrowser example (http://localhost:9004)"
#MISE depends=["build-cli"]
use ../lib.nu *
cd ($env.ROOT | path join examples filebrowser)
^($env.DIST_DIR | path join xtemplate) --config-file config.json --listen :9004
