#!/usr/bin/env nu
#MISE description="Run Go unit tests with the race detector"
use ../lib.nu *
cd $env.ROOT

^go test -race $"-coverprofile=($env.DIST_DIR)/cover.out" ./...
^go tool cover $"-func=($env.DIST_DIR)/cover.out" | lines | last
