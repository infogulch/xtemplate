#!/usr/bin/env nu
#MISE description="Run the contacts example (http://localhost:9001)"
#MISE depends=["build-cli"]
use ../lib.nu *
cd ($env.ROOT | path join examples contacts)
^($env.DIST_DIR | path join xtemplate) --config-file config.json --listen :9001
