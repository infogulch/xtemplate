{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "showLog": true,
            "program": "${workspaceFolder}/cmd/main.go",
            "env": {},
            "args": [
                "--loglevel", "4", "--listen", ":8080", "--template-dir", "./../test/templates", "--watchtemplates"
            ]
        }
    ]
}